interface:
  display_name: "Autonomous-Schematic-Generation (ASG)"
  short_description: "ASG: KiCad schematic generation & design review"
  default_prompt: "Use $autonomous-schematic-generation to build a modular circuit representation, resolve actual pins, automatically place functional blocks, preserve locked baselines, and verify native ERC, pin-by-pin nets, identity, geometry, and rendered pages."
policy:
  allow_implicit_invocation: true
